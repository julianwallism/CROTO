//nasm -f elf -o Programa4.o Programa4.asm && gcc -m32 Programa4.o -o Programa4 && ./Programa4

crotofunc int modulo(int a, int b){  
    int quocient;
    if(b<=0){
        return -1;
    }
    quocient = a/b;
    return a-b*quocient;
}

crotofunc main(){
    int a;
    int b;
    scan(a);
    scan(b);
    print(modulo(a,b));
}