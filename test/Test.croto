/* EXECUTION */
/* nasm -f elf -o Test.o Test.asm && gcc -m32 Test.o -o Test && ./Test
/* nasm -f elf -o Test.o Test.asm ; gcc -m32 Test.o -o Test ; ./Test 
 */

crotofunc int factorial(int a){
    int res = 1;
    while(a>1){
        res = res * factorial(a-1);
        a = a-1;
        print(a);
    }
    return res;
}

crotofunc main(){
    int res = factorial(7);
    print(res);
}